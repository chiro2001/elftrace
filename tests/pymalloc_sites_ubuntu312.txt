# python3.12 (Ubuntu 24.04 aarch64) pymalloc 关键普通读站点
# 格式: pc insn
# 所有分配器 pop 闭包: usedpools 头 / pool->nextpool / freeblock /
# freeblock->next; 覆盖 PyObject_Malloc/PyMem_Malloc/PyMem_Calloc/
# PyObject_Realloc 的各副本 + free/迁移路径的 freeblock/refcount 读。
# PyObject_Malloc 快路径 (主/次)
509ab0 f8627802
509ab4 f9400844
509ac0 f9400440
509ad0 f9400003
509c40 f8617802
509c44 f9400844
509c50 f9400440
509c60 f9400001
# refcount 读 (pool 满/空判定)
509ac4 b9400043
509c54 b9400041
# PyObject_Realloc 快路径
50a204 f8637843
50a208 f9400864
50a214 f9400462
50a224 f9400040
50a218 b9400060
# PyMem_Malloc 快路径
50a47c f8627802
50a480 f9400844
50a48c f9400440
50a49c f9400003
50a490 b9400043
# PyMem_Calloc 副本 1/2
50a650 f8617803
50a654 f9400861
50a660 f9400465
50a670 f94000a0
50a664 b9400060
50a868 f8617803
50a86c f9400861
50a878 f9400465
50a888 f94000a0
50a87c b9400060

# python3.12 GIL 同步站点 (ThreadingHTTPServer 多线程切片):
# take_gil 的 gil->locked 读取 (0x58b860 初始检查 + 0x58b9ac 等待循环重读)。
# 录制中主线程从不 futex_wait (只 futex_wake), take_gil 恒见 locked=0;
# 但切片因 worker 释放写进 diff 的时序不同会走进 cond_timedwait 自旋
# (该 futex 站点未记录被 mock 后无限重试)。值回放 locked=0 使切片沿
# 录制控制流走, 从不进入等待路径。注意 0x58be04/0x58be84 是 drop_gil 的
# 检查/FORCE_SWITCHING wait, 本负载中主线程不执行 drop_gil, 站点无事件。
58b860 b9400380
58b9ac b9400380
# drop_gil (PyEval_SaveThread 内联) 的 gil->locked 检查读: 录制主线程
# drop 时值=1, 切片若不回放此读, 会被边界 diff 写成的 0 触发
# "drop_gil: GIL is not locked" abort (rc=133)。值回放 1 让切片沿录制
# 控制流走; drop 的 str wzr 仍真实执行写 0, 与录制一致。
58bc78 b94002e0
# drop_gil FORCE_SWITCHING 的 gil_drop_request 读取 (ceval+4)。
# 本负载录制主线程执行 drop_gil 时读到 0 (无强制切换请求), 回放同样
# 返回 0 → 不进入 FORCE_SWITCHING cond_wait (该等待的 futex 未记录,
# mock 后无限自旋)。
58be50 b9400260
# 迁移/refill 路径 (pool 满↔可用)
509de0 f8607a81
509e18 f9400662
509e48 f8607a80
509eac f8607a80
509ed0 f9400453
509ed4 f9400260
509f24 f8607a80
509f3c f9400453
509f40 f9400260
# free 路径 (主线程把块链入 worker 改过的链)
5093a0 f9400443
5093ac b9400041
50aa94 f9400443
50aaa0 b9400041
50ad64 f9400462
50ad70 b9400061
# selectmodule poll 结果循环 (窄读: revents/fd 决定结果 list/tuple 的
# 分配序列 — 首分配分歧的上游)
6ae070 79c00c00
6ae0ac b8b96800
6ae0c4 79400c00
# PyList_New/PyList_Free 自由链表 (numfree 是 worker 共享计数器,
# 陈旧值会让切片跳过 list 分配 → 分配序列整体前移一位)
4d82a8 b9428020
4d82b4 f860d833
# _thread.lock.locked(): 读 futex 字 (普通 ldr, 非原子) — worker 结束
# 释放锁后切片仍读陈旧值 → is_alive() 误判存活 → _Threads.reap() 多
# 保留一个线程 → 多一次 GET_ITER → 分配序列前移一位 (HTTP 卡点)
46292c b9401000
# LOAD_ATTR_INSTANCE_VALUE 的 values[ix] 值加载 (eval 区) — 读
# Thread._is_stopped 等实例字典属性; worker 结束置 True 的存储若在
# 边界间被改写, 切片读到陈旧 False → reap() 误判存活 → 分配序列
# 前移一位 (HTTP 卡点)
563500 f8637841
563548 f8617801
563598 f8627801
563608 f8647861
